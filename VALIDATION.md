# 검증 기록 — 2026-09-05

- 단위·통합 테스트 5개 통과.
- 40개 정점의 모든 1,600개 출발·도착 조합: 직접 구현 다익스트라와 독립 Floyd–Warshall 결과 일치.
- 36개 실제 장소, 4개 가상 정점, 46개 양방향 간선 검증.
- 720회 무작위 배정에서 현재 위치 제외, 3개 고유 실제 장소 조건 검증.
- 잘못된 마커 처리 시 동일 상태 객체 유지, 단서 마커의 단계 해제 검증.
- 보물 3회 획득, 중복 인식 방지, 위치 갱신, 최종 교환권 저장·불러오기 검증.
- 실제 생성 QR 픽셀을 jsQR로 해독해 markerId가 일치하는지 검증.
- TypeScript 검사 및 Next.js production 정적 빌드 통과.

## 실제 브라우저 — 390 × 844

Codex 브라우저에서 실제 클릭·입력을 사용했습니다.

1. 새 게임, 닉네임 ‘길벗’, 원본 지도에서 ‘2-3’ 선택. 파란 강조와 위치 핀 확인.
2. 보물 3개 배정 화면에서 실제 장소가 표시되지 않음 확인.
3. 지도 전체/3층 탭 확인. 예시와 비교해 중앙 계단을 보완.
4. 힌트 1단계만 활성화, 2·3단계 잠김 확인.
5. 카메라 화면의 오답 마커 테스트 → 오답 모달 → 힌트 복귀 후 1단계·기존 힌트 유지 확인.
6. 당시 개발용 버튼으로 보물 3개 획득을 확인했다. 해당 버튼은 운영 전환 시 제거했으며, 아래 최신 검증에서는 실제 장소 QR 영상 인식으로 대체했다.
7. 작은 발견 상자·최종 대형 상자·황금빛 효과·교환권 버튼을 스크린샷으로 확인.
8. 교환권 689230 발급 후 새로고침: 689230 유지.
9. 메인 → 이어서 하기: 같은 교환권 689230 유지.

메인·시작·층별 지도·오답·보물 발견·최종 화면의 패널, 버튼, 배경, 상자 크기를 첨부 예시와 비교했습니다. 원본 UI 이미지의 흰 여백·화면 번호·휴대전화 외곽 프레임은 포함하지 않았습니다.

물리적 스마트폰에서 인쇄물을 비추는 현장 카메라 검증은 수행하지 않았습니다. 카메라 화면 연결과 QR 디코더의 실제 이미지 해독은 각각 검증했습니다.

## 카메라 AR 확인 단계 수정

- 기존 자동 테스트 8개 통과.
- 브라우저에서 정상 보물 마커 테스트 후 /game/ar/에 머무르고 AR 발견 확인 영역과 상자가 표시됨을 확인.
- ‘확인’을 누른 뒤에만 /game/result/로 이동하고 첫 보물 획득 표시 확인.
- 다음 보물의 단서 마커 테스트 후 카메라 확인 화면 유지, ‘다음으로’를 누른 뒤 2단계 힌트가 열림 확인.
- 카메라 컴포넌트는 확인 대기 중 재마운트하지 않으며, 화면에서 나갈 때 미디어 트랙을 정리.
- 스마트폰 현장 카메라 검증은 별도 필요.

## QR 평면 추적 및 단서 이미지

총 12개 테스트 통과: 이동·크기·회전·원근 변환의 네 모서리 일치, 세로 카메라 cover 잘림 보정, 인쇄 QR 모서리 해독, 연속 프레임에서 1회 게임 판정, QR 소실과 재인식 복귀 검증을 추가했습니다. 단서 이미지는 보물상자와 별개 PNG로 생성·확인했습니다. 실제 스마트폰에서 움직이는 QR을 비추는 현장 확인은 필요합니다.

## A* 전환

- 15개 테스트 통과. 모든 40 × 40 경로 거리와 독립 Floyd–Warshall 결과 일치.
- 모든 목적지에서 직선거리 휴리스틱의 과대 추정 방지와 간선별 일관성 검증.
- 36 × 36 실제 장소 경로가 실제 간선으로 구성되고 가중치 합이 맞으며 3단계 힌트와 연결됨을 확인.
- 새 게임·다음 보물에서 A* 결과와 단서 마커 연결 검증.
- 지도 데이터·그래프·화면·AR 구현은 변경하지 않음.


## 운영 전환 검증 — 2026-09-09

- 공용 QR 진행 어댑터, 활성화 설정, 생성 스크립트, 인쇄 페이지·PNG, 개발용 버튼·스타일·사용 안내 제거.
- 운영 게임은 기존 scanMarker로 실제 장소 마커와 현재 단서 순서를 판정한다. 기존 보물 마커를 바로 찾는 동작도 유지한다.
- 회귀 테스트 24개 통과: A* 경로, 단계별 단서, 보물 3개, 중복 획득 방지, QR 위치 추적, 닉네임 중복·완료·쿠폰 처리.
- 36개 출발 장소에서 공용 QR이 각 힌트 단계·보물 단계의 상태를 바꾸지 않는지 검증. 실제 인쇄 QR 36개 모두 해당 장소 ID로 해독됨.
- TypeScript 검사 및 Next.js 운영 정적 빌드 통과. 출력물에도 폐기된 QR PNG가 없음.
- Chrome 브라우저 전체 흐름 통과: 실제 장소 PNG → 카메라 영상 → jsQR 인식 → AR 확인 → 보물 3개 → 완료 저장 실패/재시도 → 쿠폰 유지·조회. 닉네임 중복, 통신 오류, 연속 클릭, 새로고침, 모바일·데스크톱 폭도 검증.
- 브라우저 검증의 RPC 응답은 모의 응답을 사용했다. Supabase DB·정책·환경 변수·저장 코드는 변경하지 않았다. 실물 스마트폰 현장 촬영은 수행하지 않았다.
- 브라우저 카메라 입력 도우미는 tests/에만 있으며 게임 번들에 포함되지 않는다.

재실행: `node --import tsx --test tests/*.test.ts`, `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/next/dist/bin/next build`.
브라우저 검증: Supabase 공개 환경 변수가 설정된 로컬 개발 서버 실행 후 `node tests/players.browser.mjs`.
이번 Windows 환경에서는 pnpm의 자동 설치 확인이 중단되어 설치된 실행 파일로 검증했고, tsx 사용자 정보 조회 제한을 해결하기 위해 승인된 환경에서 테스트를 실행했다.
